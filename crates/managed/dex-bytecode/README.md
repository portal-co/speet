# dex-bytecode

Zero-dependency DEX (Dalvik Executable) bytecode parser. Decodes all standard DEX opcodes and pseudo-instructions into fully typed Rust variants. No unsafe code.

Used by `speet-dex` to feed decoded instructions into the recompiler pipeline.

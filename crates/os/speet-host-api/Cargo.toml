[package]
name = "speet-host-api"
version = "0.1.0"
edition = "2021"
description = "Pluggable host API for the thin runtime: tunneled libc/syscall boundary."

[dependencies]
binary-io = { path = "../../../../binary-io" }
tunnel = { path = "../../../../tunnel" }
osctx = { path = "../osctx" }

[dev-dependencies]

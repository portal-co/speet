[package]
name = "speet-linux-wasi-guest"
version = "0.1.0"
edition = "2021"
publish = false
description = "Linux→WASI syscall handlers compiled to WASM (pre-shimming host-mem imports)"

[lib]
crate-type = ["cdylib"]

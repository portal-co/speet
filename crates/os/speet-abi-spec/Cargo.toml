[package]
name = "speet-abi-spec"
version = "0.1.0"
edition = "2021"
description = "Compatibility shim for `os-abi-spec`."
publish = false
license-file = "../../../COPYING.md"

[dependencies]
os-abi-spec = { path = "../../../../os-emulation/crates/abi/os-abi-spec" }

[package]
name = "speet-host-syscall"
version = "0.1.0"
edition = "2021"
description = "Host-ambient syscall dispatch for the speet full-binary recompiler (libc syscall / libSystem instead of WASI)."

[dependencies]

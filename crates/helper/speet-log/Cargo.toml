[package]
name = "speet-log"
version = "0.1.0"
edition = "2024"
publish.workspace = true

[dependencies]
log = { version = "0.4", optional = true, features = ["std"] }

[features]
log-subscriber = ["dep:log"]

//! [`LocalLayout`] and [`LocalSlot`] — re-exported from `yecta`.
//!
//! These types are defined in `yecta::layout` and re-exported here for
//! backward compatibility.  New code should prefer importing them from `yecta`
//! directly.

pub use yecta::{LocalLayout, LocalSlot, Mark};
